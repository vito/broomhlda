here = File expand-path("../", _FILE)

require(here + "/macros")
require(here + "/token")
require(here + "/state")
require(here + "/lexers")
require(here + "/formatters/html")

load(lexer) :=
  require(
    File expand-path(
      "../lexers/" + lexer downcase
      _FILE
    )
  )
