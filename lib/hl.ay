here = File expand-path("../", _FILE)
$path unshift(here)

namespace(broomhlda)

import("macros")
import("token")
import("state")
import("lexers")
import("formatters/html")

load(lexer) :=
  import("lexers/" + lexer downcase)
